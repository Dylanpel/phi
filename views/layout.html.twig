<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <base href="{{ baseUrl }}/">
  <title>{% block title %}Pharmacie Humanitaire Internationale{% endblock %}</title>
  <link rel="icon" type="image/svg" href="favicon.svg">

  {% set bsCssFile = app_env == 'dev' ? 'bootstrap.css' : 'bootstrap.min.css' %}
  {# {% set bsCssFile = 'bootstrap.min.css' %} #}
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/{{ bsCssFile }}" crossorigin="anonymous">
  <link rel="stylesheet" href="https://bootswatch.com/5/minty/{{ bsCssFile }}">
  <link rel="stylesheet" href="css/variables.css">
  <link rel="stylesheet" href="build/css/style.css">

  {% block stylesheets %}{% endblock %}
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container">
      <a class="navbar-brand" href="#">MonSite</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="{{ url('') }}">Accueil</a></li>
          <li class="nav-item"><a class="nav-link" href="#">Contact</a></li>
          {% if isAdmin %}
            <li class="nav-item"><a class="nav-link" href="{{ url('register') }}">Créer un utilisateur</a></li>
          {% endif %}
        </ul>

        <div class="d-flex align-items-center gap-2">
          {% if isLoggedIn %}
            <a href="{{ url('logout') }}" class="btn btn-outline-light btn-sm">Déconnexion</a>
          {% else %}
            <a href="{{ url('login') }}" class="btn btn-outline-light btn-sm">Connexion</a>
          {% endif %}

          {% if currentUser %}
            <small class="navbar-text me-3">
              <strong>{{ currentUser.login }}</strong><br>
              Rôle : {{ currentUser.role }}
            </small>
          {% endif %}
        </div>
      </div>
    </div>
  </nav>

  <div style="padding-top: 70px;"></div>

  <main class="container py-4">
    {% block content %}{% endblock %}
  </main>

  <footer class="bg-dark text-white py-4 mt-auto">
    <div class="container text-center">
      <p class="mb-0">&copy; {{ "now"|date("Y") }} Pharmacie Humanitaire Internationale. Tous droits réservés.</p>
    </div>
  </footer>

  {% set bsJsFile = app_env == 'dev' ? 'bootstrap.bundle.js' : 'bootstrap.bundle.min.js' %}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/{{ bsJsFile }}" crossorigin="anonymous"></script>
  <script src="js/script.js"></script>

  {% block scripts %}{% endblock %}
</body>
</html>