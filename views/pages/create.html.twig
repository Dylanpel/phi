{% extends 'layout.html.twig' %}

{% block title %}Création de page{% endblock %}

{% block content %}
  
  {% if error is defined %}
    <div class="alert alert-danger">{{ error }}</div>
  {% endif %}

  <div class="row justify-content-center mt-5">
    <div class="col-md-8">
      <h3 class="text-center mb-4">Création d'une page</h3>

    </div>
  </div>
{% endblock %}